/**
 * Get all prices in Euros
 *
 * @param {*} - Can be a string, number, array or object
 * @returns - return a value as the same type as argument with prices only.
 */
function getPrices(data) {
  const pattern = /[\d'., ]{2,}/g;
  // If string or number
  if (typeof data === "string" && data.length > 0) {
  }

  if (typeof data === typeof 0 && data.length > 0) {
  }

  if (typeof data === "string" && data.length > 0) {
  }

  if (typeof data === "string" && data.length > 0) {
  }
}

function isString(str) {}
function isNumeric(num) {}
function isArray(arr) {}
function isObject(obj) {}

// function isString() {}
// function isNumeric() {}
// function isArray() {}
// function isObject() {}
